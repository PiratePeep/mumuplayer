# Type Alias: NetBridgeIpMode

> **NetBridgeIpMode** = `"dhcp"` \| `"static"`

Defined in: [src/types.ts:433](https://github.com/PiratePeep/mumuplayer/blob/main/src/types.ts#L433)

Network bridge IP assignment mode.

- `"dhcp"` — obtain IP automatically
- `"static"` — use manually configured IP settings
